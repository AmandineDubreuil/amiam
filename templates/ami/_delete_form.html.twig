<form method="post" action="{{ path('app_ami_delete', {'id': ami.id}) }}" onsubmit="return confirm('Es-tu sûr(e) de vouloir supprimer cette fiche ami ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ami.id) }}">
<button class="btn iconeSupprimer"><i class="fa-solid fa-trash-can"></i></button>
</form>

{% extends 'base.html.twig' %}

{% block title %}RecetteIngredient index{% endblock %}

{% block body %}
    <h1>RecetteIngredient index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantite</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for recette_ingredient in recette_ingredients %}
            <tr>
                <td>{{ recette_ingredient.id }}</td>
                <td>{{ recette_ingredient.quantite }}</td>
                <td>
                    <a href="{{ path('app_recette_ingredient_show', {'id': recette_ingredient.id}) }}">show</a>
                    <a href="{{ path('app_recette_ingredient_edit', {'id': recette_ingredient.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_recette_ingredient_new') }}">Create new</a>
{% endblock %}

{{ form_start(form) }}
{# <div class="form-field">
	{{ form_row(form.date, {
            label: 'Date du repas *',
                   }) }}
</div> #}
<div class="form-field">
	{{ form_row(form.commentaire, {
            label: 'Commentaire ',
                   }) }}
</div>
<div class="groupeBtn">
	<button class="btn iconeOk">
		<i class="fa-solid fa-check"></i>
	</button>
</div>
{{ form_end(form) }}

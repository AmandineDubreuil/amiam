<form method="post" action="{{ path('app_recette_delete', {'id': recette.id}) }}" onsubmit="return confirm('Es-tu sûr(e) de vouloir supprimer cette recette ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recette.id) }}">
<button class="btn iconeSupprimer"><i class="fa-solid fa-trash-can"></i></button>
</form>

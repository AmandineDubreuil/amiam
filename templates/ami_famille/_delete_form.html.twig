<form method="post" action="{{ path('app_ami_famille_delete', {'id': ami_famille.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ami_famille.id) }}">
    <button class="btn">Delete</button>
</form>

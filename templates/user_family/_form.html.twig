{{ form_start(form) }}
<div class="form-field">
	{{ form_row(form.prenom, {
            label: 'Prénom',
                   }) }}
</div>
<div class="form-field">
	{{ form_row(form.dateNaissance, {
            label: 'Date de naissance',
                   }) }}
</div>

    <button class="btn btnOk">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}

<form method="post" action="{{ path('app_user_family_delete', {'id': user_family.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_family.id) }}">
    <button class="btn">Delete</button>
</form>
